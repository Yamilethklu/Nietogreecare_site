import { z } from 'zod';
const uuid=z.string().uuid();
export const workerSchema=z.object({full_name:z.string().trim().min(2).max(120),email:z.string().trim().toLowerCase().email(),phone:z.string().trim().max(30).nullable().default(null),active:z.boolean().default(true)});
export const planSchema=z.object({lead_id:uuid,crew_member_id:uuid.nullable(),cadence:z.enum(['weekly','bi_weekly','one_time']),first_date:z.string().regex(/^\d{4}-\d{2}-\d{2}$/),preferred_start:z.string().regex(/^([01]\d|2[0-3]):[0-5]\d$/),duration_minutes:z.number().int().min(15).max(480),price_per_visit:z.number().min(0).max(100000),notes:z.string().trim().max(2000).nullable().default(null)});
export const manualLeadSchema=z.object({customer_name:z.string().trim().min(2).max(120),customer_phone:z.string().trim().min(7).max(30),customer_email:z.string().trim().email().nullable(),address:z.string().trim().min(5).max(250),city:z.string().trim().min(2).max(90),zip_code:z.string().regex(/^\d{5}$/)});
export const orderUpdateSchema=z.object({id:uuid,status:z.enum(['scheduled','in_progress','completed','cancelled']).optional(),paid_amount:z.number().min(0).max(100000).optional(),payment_method:z.enum(['cash','cash_app','venmo','zelle','check','other']).nullable().optional(),notes:z.string().trim().max(2000).nullable().optional(),crew_member_id:uuid.nullable().optional(),service_date:z.string().regex(/^\d{4}-\d{2}-\d{2}$/).optional(),start_time:z.string().regex(/^([01]\d|2[0-3]):[0-5]\d$/).optional(),duration_minutes:z.number().int().min(15).max(480).optional()});
